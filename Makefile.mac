#!/usr/bin/make -f
CC = g++
CFLAGS  = -Wall -g
PROG = Game

SRCS = main.cpp Ship.cpp

LIBS =  -framework OpenGL -framework GLUT -Wno-deprecated-declarations

all: $(PROG)

$(PROG):	$(SRCS)
	$(CC) $(CFLAGS) -o $(PROG) $(SRCS) $(LIBS)

clean:
	rm -f $(PROG) 

